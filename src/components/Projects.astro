---
import Project from '../components/Project.astro';
let posts = await Astro.glob('../pages/blog/*.mdx');
posts.sort((a, b) => new Date(b.frontmatter.pubDate).getTime() - new Date(a.frontmatter.pubDate).getTime());
---
<div class="hero">
    <h1>Blog</h1>
    <p>This is the space where I showcase the work I've done in a (hopefully) digestable way!</p>
    <ul>
    {posts.map((post) => (
        <Project
            title={post.frontmatter.title}
            actions={post.frontmatter.actions}>
          <img src={post.frontmatter.previewImage} alt={post.frontmatter.title} slot="header"/>
          {post.frontmatter.description}
        </Project>
    ))}
    </ul>
</div>
<style>
    .hero{
        position: absolute;
        display: flex;
        left: 50%;
        top: 10%;
        flex-direction: column;
        transform: translateX(-50%);
        gap: -5px;
        color: white;
    }
    ul {
        margin: 0;
        padding-left: 0;
        list-style: none;
    }
    .p{
        position: relative;
        width: 760px;
        height: 88px;
        
    }
    @media screen and (max-width: 900px) {
        .hero {
            width: 90%;
        }
    }
    @media screen and (max-width: 636px) {
        h1 {
            font-size: 40px;
        }
        p {
            font-size: 14px;
            line-height: 1.3;
        }
    }
</style>
